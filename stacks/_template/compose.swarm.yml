services:
  app:
    deploy:
      replicas: 1
      restart_policy:
        condition: any
      update_config:
        order: start-first
        failure_action: rollback
      labels:
        oht.stack: template

networks:
  app:
    driver: overlay
    attachable: true
