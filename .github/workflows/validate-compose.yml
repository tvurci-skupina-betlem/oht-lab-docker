name: Validate Compose

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  compose:
    name: Compose config
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Validate compose files
        run: sh ./scripts/validate-compose.sh
