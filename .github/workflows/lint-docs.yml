name: Lint Docs

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  markdown:
    name: Markdown
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Lint Markdown
        uses: DavidAnson/markdownlint-cli2-action@v23
        with:
          config: "custom.markdownlint-cli2.jsonc"
          globs: |
            README.md
            SECURITY.md
            docs/**/*.md
            komodo/**/*.md
            stacks/**/*.md
