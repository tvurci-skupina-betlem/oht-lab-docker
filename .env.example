# Repository-wide defaults.
# Copy stack-specific .env.example files next to their compose.yml files when deploying manually.

TZ=Europe/Prague
PUID=1000
PGID=1000

# Example shared network name for reverse-proxied services.
OHT_PROXY_NETWORK=oht_proxy
